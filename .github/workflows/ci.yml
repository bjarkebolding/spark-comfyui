name: ci
on:
  push:
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: >
          shellcheck -S warning spark-comfyui.sh mods/*/run.sh
          mods/_lib/mod_common.sh container/*.sh
